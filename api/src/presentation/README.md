# Presentation

HTTP-specific controllers, request parsing, response DTO mapping, and error
mapping live here. Presentation may depend on application use cases and shared
API contracts, but domain and application code must not depend on this layer.
